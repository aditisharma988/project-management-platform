# Set the server port for Eureka Dashboard
server.port=8761

# Name of the application
spring.application.name=service-registry

# Tell Eureka Server not to register itself as a client
eureka.client.register-with-eureka=false

# Tell Eureka Server not to fetch registry details from outside
eureka.client.fetch-registry=false